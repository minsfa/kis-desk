# kis-desk 상태 압축 — 2026-06-18

KIS 투자 코파일럿. 텔레그램(OpenClaw/madu_bot)에서 묻고·답받고·필요시 매매.
GitHub: **github.com/minsfa/kis-desk (Public)** · 로컬 `~/kis-autotrade`.

## 두 엔진 (현황)
### 🔬 리서치 엔진 (가동) — KR 가치 + 성장 렌즈
| 명령 | 용도 |
|---|---|
| `holdco` / `nav [--wide] [--top N]` / `navdetail <c>` | 지주사 PBR·시총 / NAV 할인율(시총 vs 상장지분) |
| `overlay` / `quality` / `scorecard [--wide]` | 외국인수급·자사주공시 / ROE·재무 / 종합(🪤 가치함정) |
| `growth <c>` | 성장 렌즈: 매출CAGR·PSR·PEG·15%CAGR허들 (정량만) |
| `portfolio` | 보유 포트폴리오 점검(읽기전용) |
| `fundamentals/diagnose/dart/news <c>` | 단일 종목 |
- 핵심: 장부 PBR은 지주사에 안 맞음→**NAV가 척도**, 단 NAV만 싼 건 **가치함정**(ROE로 거름). 정성(TAM·해자·라이트의법칙)은 수치화 안 함→사람 딥리서치.
- 문서: `docs/HOLDCO_RESEARCH.md` · `docs/METRICS_FRAMEWORK.md`(가치🟦+성장🟧 지표사전)

### ⚙️ 실행 엔진 — US H1 하버스 **무인 가동 중** ✅
- `usstab buy|sell|report [--live]` (`src/kis/us_stab.py`): 기계적 왕복(SOFI 1주) 안정성 테스트.
- 멱등(잔고체크)·circuit breaker·일일손실한도·지수백오프·에러분류·지표CSV+report. 브로커=ground truth.
- **openclaw cron 3개 가동**: buy 22:35(월~금) / sell 04:50(화~토) / report 05:10 — 텔레그램 보고.
- 라이브 검증완료(SOFI 왕복, report 집계). KR 실행엔진(strat_v0/propose/approve)은 6/5 이후 휴면.

## 계좌 전략 (7월 테스트 후 증액/계좌 결정)
- **자동매매 = US**(격리·소액 1주, 통합증거금 자동환전으로 원화→달러자산). **모니터링 = 메인계좌**(읽기전용 `portfolio`).
- 연금/ISA = KIS OpenAPI 매매 대부분 미지원(확인필요), 자동매매 대상 아님.

## ⚠️ 운영 주의
- **중단**: `touch ~/kis-autotrade/STOP` / `openclaw cron disable <id>` / `.env DRY_RUN=true`.
- **브랜치 의존**: cron이 작업트리 실행 → `feat/us-stability-test` 체크아웃 유지 필요(main엔 us_stab.py 없음). → PR머지로 견고화.
- **DST**: cron시간 미국 EDT기준. 11월 EST전환시 buy 23:35/sell 05:50 조정.
- OpenClaw: google-workspace MCP 비활성화함(로그인루프 제거). 맥미니 sleep 0(안 잠). heartbeat 모델 qwen(4K)이라 가끔 실패 — 기능 영향 작음.

## 브랜치/PR (둘 다 머지 대기)
- `feat/analysis-lenses`: growth.py + portfolio.py + METRICS_FRAMEWORK (커밋 4d06a6c, 5f8e113)
- `feat/us-stability-test`: us_stab.py H1 하버스 (커밋 e7d9fab) ← 현재 체크아웃
- main = 604cf6a (단일 클린 커밋, 계좌번호 등 민감정보 purge 후 공개)

## 미정/다음
1. PR 머지 2건(특히 us-stability-test → cron 견고화)
2. US H1 며칠 데이터 누적 → `report` 안정성 추세 → 증액 판단
3. 중국 ETF: 국내상장 12개 발굴(본토=고점 강세/홍콩=저점 약세 분화), 정확한 기간수익률은 일봉 산출 가능(미작업)
4. (옵션) 정성 6요소 브리프, scorecard 성장점수 통합, 11월 DST 조정
